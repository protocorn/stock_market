import 'package:flutter/material.dart';

class SmallGapHorizontal extends StatelessWidget {
  const SmallGapHorizontal({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10);
  }
}