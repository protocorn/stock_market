import 'package:flutter/material.dart';

class SmallGapVertical extends StatelessWidget {
  const SmallGapVertical({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10);
  }
}
